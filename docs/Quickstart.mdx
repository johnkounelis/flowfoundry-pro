# Quickstart

1. `docker compose -f infra/docker-compose.yml up -d`
2. `pnpm i`
3. `pnpm dev:bootstrap`
4. `pnpm dev`
5. Open http://localhost:3000 and sign in with Email (Mailhog: http://localhost:8025)
