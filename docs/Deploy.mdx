# Deploy

- Web on Vercel or Kubernetes.
- Worker on Fly.io/Render/Kubernetes.
- Postgres: Neon (serverless) or managed Postgres.
- Redis: Upstash.
- Object storage: S3-compatible.
- Configure secrets per `.env.example`.
