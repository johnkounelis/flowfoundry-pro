apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - web.yaml
  - worker.yaml
  - postgres.yaml
  - redis.yaml
  - ingress.yaml
configMapGenerator:
  - name: app-config
    literals:
      NEXT_PUBLIC_DEFAULT_LOCALE=en-US
